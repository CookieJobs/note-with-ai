import { fireEvent, render, screen, waitFor } from '@testing-library/react'; import { vi, it, expect } from 'vitest'; import Users from './page'; import * as api from '../lib/adminApi';
it('searches only after submit and sends canonical query', async () => { const fetch = vi.spyOn(api, 'adminFetch').mockResolvedValue({ items: [{ id: 'u', username: 'alice', maskedEmail: 'a***e@example.com', email: 'private', isActive: true, noteCount: 1, aiCalls30d: 2 }], pagination: { page: 1, limit: 20, total: 1, hasNext: false } } as any); render(<Users />); await waitFor(() => expect(screen.getByText('alice')).toBeInTheDocument()); expect(fetch).toHaveBeenCalledTimes(1); fireEvent.change(screen.getByLabelText('搜索用户'), { target: { value: 'alice' } }); expect(fetch).toHaveBeenCalledTimes(1); fireEvent.click(screen.getByRole('button', { name: '搜索' })); await waitFor(() => expect(fetch).toHaveBeenCalledTimes(2)); expect(fetch).toHaveBeenLastCalledWith(expect.stringContaining('query=alice')); expect(screen.queryByText('private')).not.toBeInTheDocument(); });
