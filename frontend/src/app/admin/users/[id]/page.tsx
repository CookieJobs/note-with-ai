'use client'; import { useEffect, useState } from 'react'; import { useParams } from 'next/navigation'; import { adminFetch } from '../../lib/adminApi'; import styles from '../../admin.module.scss';
export default function UserDetailPage() { const { id } = useParams<{id:string}>(); const [user, setUser] = useState<any>(); useEffect(() => { adminFetch<any>(`/api/admin/users/${id}`).then(setUser); }, [id]); if (!user) return <p>加载用户中…</p>; return <section><h2>{user.username}</h2><div className={styles.card}><p>邮箱：{user.email || user.maskedEmail}</p><p>状态：{user.isActive ? '正常' : '已禁用'}</p><p>笔记：{user.noteCount} · 聊天：{user.chatCount} · AI：{user.aiCalls30d}</p></div></section>; }
