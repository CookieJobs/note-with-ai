'use client';
import { useEffect, useRef, useState } from 'react';
export default function ReasonDialog({ onSubmit, onClose }: { onSubmit: (reason: string) => Promise<void>; onClose: () => void }) { const [reason, setReason] = useState(''); const [pending, setPending] = useState(false); const [error, setError] = useState(''); const dialog = useRef<HTMLDivElement>(null); const previous = useRef<HTMLElement | null>(null); const valid = reason.trim().length >= 5 && reason.trim().length <= 200; useEffect(() => { previous.current = document.activeElement as HTMLElement; dialog.current?.querySelector('textarea')?.focus(); const key = (e: KeyboardEvent) => { if (e.key === 'Escape' && !pending) onClose(); if (e.key === 'Tab' && dialog.current) { const focusable = Array.from(dialog.current.querySelectorAll<HTMLElement>('textarea,button:not([disabled])')); const first = focusable[0], last = focusable[focusable.length - 1]; if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last?.focus(); } else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first?.focus(); } } }; document.addEventListener('keydown', key); return () => { document.removeEventListener('keydown', key); previous.current?.focus(); }; }, [pending, onClose]); return <div ref={dialog} role="dialog" aria-modal="true" onKeyDown={e => { if (e.key === 'Escape' && !pending) onClose(); }}><label>原因<textarea value={reason} onChange={e => { setReason(e.target.value); setError(''); }} /></label>{error && <p role="alert">{error}</p>}<button disabled={!valid || pending} onClick={async () => { setPending(true); setError(''); try { await onSubmit(reason.trim()); } catch (e: any) { setError(e.message || '提交失败'); } finally { setPending(false); } }}>{pending ? '提交中…' : '确认'}</button><button disabled={pending} onClick={onClose}>取消</button></div>; }
