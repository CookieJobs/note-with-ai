# Task 14 report — acceptance gates and final verification

## Result

Added executable axe and route-budget gates, then used their RED evidence to correct the smallest upstream defects required for a green acceptance run. This fix round also hardens schema-free rich-draft recovery, replaces the Notes delete overlay with the shared dialog contract, and broadens style/motion contracts. The production `/notes` first-load budget remains 231 kB, below the binding 300 kB cap and 45.1% below the 421 kB reference.

## TDD evidence

- Initial `npm run test:a11y` failed because the required test file did not exist; `npm run check:route-budgets` failed because the parser did not exist.
- The parser test initially failed to resolve `check-route-budgets.mjs`; after implementation, its 3 cases passed, including absent `/notes`, kB/MB parsing, absolute cap, and reduction floor.
- The first axe run found the invalid Auth `form[role=tabpanel]` and Note `menuitem[aria-pressed]`. Focused Auth/Card regressions failed before the fixes and passed afterward.
- The bundle guard initially exposed 470 kB First Load JS. Source-contract RED tests then failed while the Notes entry retained the Tiptap schema/viewer. Moving those imports across the existing dynamic boundary reduced First Load JS to 231 kB; draft recovery and Notes suites stayed green.
- The rich-draft validator RED cases showed that a type-name whitelist accepted direct document text, marks on block nodes, nested block/inline violations, and non-leaf content. The schema-free validator now mirrors the supported ProseMirror placement, leaf, content, and mark rules without importing Tiptap into the route bundle; its valid boundary document remains accepted.
- Fix round 2 adds StarterKit's configured `underline` mark to that exact whitelist and proves underline restoration has no recovery error. It also rejects empty text leaves. A source-backed test names every persisted node/mark supplied by StarterKit plus the explicit Link, Task, image, code-block, highlight, and table extensions; History, Markdown, Placeholder, TextAlign, and cursor plugins intentionally add no persisted node/mark names.
- The Notes delete confirmation RED cases showed a non-modal custom overlay. It now composes the shared dialog primitive, including a labelled modal role, focus lifecycle, Escape/overlay close policy, deterministic opener-focus restoration, removed closed DOM, 44px actions, and an axe assertion. Memory's delete dialog is now covered by axe as well.
- Style-contract RED cases exposed raw hex/RGB/RGBA/HSL/HSLA business colors and three `transition: all` declarations. Fix round 2 extends the contract to exact in-scope core-route TSX sources and includes negative hex/RGB/RGBA/HSL source cases. Notes compose/page and active Chat components now consume semantic/component tokens, transitions name their properties, and affected motion has a reduced-motion path.
- Fix round 4 began with root's production-browser `getBoundingClientRect` matrix on a 1000-note account at 320/390/768/1440px. RED target contracts named the measured undersized controls: the shared brand link (27px tall), Notes quick-compose opener (24px) and add-keyword controls (24px), Chat refresh (36px) and note-source action (18px), Inspiration actions (37px), and Profile edit/password/update/collapse controls (36px). The GREEN implementation gives those controls 44px physical targets; the Inspiration native checkbox remains 18px because its associated `.consent` label is the actual 44px-plus target.
- The same approved seven-route source scan found analogous compact controls in Auth mode/password/footer actions, Memory edit/delete/evidence actions, Publish card/preview actions, TopNavigation account/menu actions, Chat drawer/delete actions, Notes rich-editor/bubble/URL/compose actions, and RelatedNoteCard expansion. Those actionable controls now meet the 44px product baseline while their visible glyphs and text remain compact. Decorative icons, skeletons, status badges, and the labelled native checkbox were not changed.
- Fix round 5 corrected the remaining keyword-chip P1: a `span[role=button]` contained its native delete button, and the enlarged absolute delete target could overlap adjacent chips. Each chip is now a noninteractive, normal-flow grid wrapper with sibling named edit/delete buttons, each at least 44px; the edit column wraps long labels and the fixed delete column reserves its own space. Focused DOM tests prove there is no nested interactive control, keyboard edit/delete works, delete does not bubble to the card, and the style contract rejects an absolute delete target.
- Final independent review reopened the release for unreachable system-dark tokens, a mouse-only Care Assistant CTA, an unmanaged URL overlay, raw Tailwind palette utilities missed by the style gate, unconsumed batched `aiIncluded` values, a related-note revision race, and an unbounded cached-candidate query. The closing fixes activate system dark through CSS, migrate both interactions to semantic shared primitives, expand the palette gate, eliminate the per-card preference N+1 path, reject stale relationship responses by source revision, and cap stable-ranked Mongo candidates at 20 before returning at most five summaries.

## Acceptance evidence

The final root run passed frontend lint, typecheck, 40 Vitest files / 295 tests, production build, axe, route budget, and `git diff --check`; backend passed 169 tests, typecheck, and build. The `/notes` result remained 231.0 kB First Load JS, 45.1% below the reference.

## Ruling and cost

Task 14 was reopened rather than waiving the generated acceptance defects. This round's ruling is to preserve the dynamic editor split while making the static draft validator accurately enforce the editor's supported grammar, to use the shared dialog contract for destructive Notes actions, and to reject non-token business colors and broad transitions.

Cost: the validator duplicates the supported static grammar without a runtime Tiptap import; delete intent now carries a persistent opener ref from menu to card to page; and active Notes/Chat styles were migrated to component tokens with property-specific transitions. The rejected alternatives—adding Tiptap to the entry bundle, retaining a bespoke overlay, or broadly allowlisting raw colors—would violate the binding budget, accessibility, or style-contract requirements. Task 12/13 behavior remains covered by the full suite.

The round-4 cost is deliberately larger touch boxes around dense controls, with visual glyph/text scale left unchanged. The rejected alternative—treating the small icon or intrinsic checkbox as the target, or preserving the measured compact buttons—is inconsistent with this product's 44×44px baseline. The source contract is intentionally scoped to the approved seven-route matrix; it does not make a repository-wide claim or modify excluded Admin/public work.

The round-5 cost is a two-control keyword row instead of the previous single clickable chip plus a hovering delete affordance. This explicitly trades a little horizontal density for valid semantics, non-overlapping targets, keyboard operation, and predictable wrapping on narrow screens; retaining nested interactivity or an overlapping absolute target was rejected.

The raw-color gate is deliberately not an absolute repository-wide claim. It scans SCSS plus direct TSX style sources for the seven Task 14 matrix routes (Notes, Chat, Memory, Inspiration, Profile, Publish, Auth) and their listed shared route components. The direct-page manifest is regression-checked against those seven names and explicitly includes Publish's `/publish`, `/publish/select`, and `/publish/[noteId]` pages. It excludes `/p/[slug]`, which belongs to the separate public-sharing route rather than the Task 14 matrix or Task 9 core-route file list; `/admin/**`, which is both outside this frontend-remediation scope and user-owned; and the Profile atmosphere resolver's generated HSL values, which are separately constrained to the three decorative atmosphere variables. Dynamic editor configuration is schema-tested separately and has no runtime validator import.

## Visual/audit evidence and limitation

Root seeded isolated 24-, 251-, and 1000-note local accounts and completed the post-fix browser matrix. Notes, Chat, Memory, Inspiration, Profile, and Publish passed 320/390/768/1440px without horizontal overflow; all visible in-scope anchors/buttons measured at least 44×44px after remediation. The final 320px Profile actions measured 111.5×44px each and fit within the 305px content viewport. Auth passed its 320px compact layout and ArrowRight roving-tab path. The 1000-note account preserved a 30-note initial page and appended to 60 without duplicates; the 251-note account reached exactly 251 unique notes, terminal pagination state, and no overflow. Cool-blue, nature-green, and very-dark-purple Profile inputs produced three visibly distinct decorative atmospheres without recoloring semantic surfaces.

Root also exercised the Notes destructive dialog and mobile Chat drawer through the live accessibility tree: labelled modal state appeared, Escape removed the closed tree, and focus returned to the exact opener. Actual macOS system-dark appearance and VoiceOver audio remain pending because the in-app browser exposes no theme/zoom emulator and changing user OS settings requires explicit authorization. The 320/390 runs provide equivalent narrow reflow evidence for 200% layout, and automated dark/reduced-motion/contrast contracts plus accessibility-tree keyboard checks cover the deterministic behavior available without an OS mutation. Impeccable's prescribed bootstrap was blocked by the missing `PRODUCT.md`; creating that product artifact remains outside Task 14 scope.
